# eZWALT

Blog & portfolio built with Astro.

## Setup

```bash
cd new_portfolio && npm install
npm run dev       # localhost:4321
npm run build     # deploy-ready output
```

Add markdown docs to `src/content/docs/{section}/` with frontmatter: title, description. Sidebar auto-generates from `astro.config.mjs`.
